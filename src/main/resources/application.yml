spring:
  datasource:
    url: jdbc:h2:mem:stayease
    driver-class-name: org.h2.Driver
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

server:
  port: 10000
